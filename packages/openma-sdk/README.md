# OpenMA SDK snapshot

Unmodified SDK source from `openma-ai/open-managed-agents`, commit
`f35a84ef209c1f48113c8a9d003e1da6c4eea690`, `packages/sdk/src` (version 1.0.0-beta.1).
The npm release currently predates the v1 API. This workspace snapshot keeps
Backchat builds independent of a sibling checkout until that release is published.

Only packaging differs: the desktop build bundles these TypeScript entrypoints.
Update the source as a unit from upstream; do not fork the SDK resource contracts.
